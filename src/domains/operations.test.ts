import { describe, expect, it } from "vitest";
import { StoreOperationsFirestore } from "./operations.js";

describe("StoreOperationsFirestore", () => {
	it("should be defined", () => {
		expect(StoreOperationsFirestore).toBeDefined();
	});
});
