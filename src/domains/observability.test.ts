import { describe, expect, it } from "vitest";
import { ObservabilityFirestore } from "./observability.js";

describe("ObservabilityFirestore", () => {
	it("should be defined", () => {
		expect(ObservabilityFirestore).toBeDefined();
	});
});
